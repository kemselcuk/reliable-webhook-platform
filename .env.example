# Local-only defaults for compose.yaml. Never put real credentials in this file.
POSTGRES_DB=webhook
POSTGRES_USER=webhook
POSTGRES_PASSWORD=webhook-local-only
POSTGRES_PORT=5432
KAFKA_PORT=9092
BACKEND_PORT=8080
FRONTEND_PORT=3000
